# Bugs

|| ??queryFields key must be column alias, because there might
columns with same name:?? ! maybe not, bc I think it's already per class
queryFields = {ColumnName[name=author_name]=ColumnField[columnAlias=b.author_name, field=Field[name=authorName, type=java.lang.String]], ColumnName[name=name]=ColumnField[columnAlias=b.name, field=Field[name=name, type=java.lang.String]]}

|| alias might not be a simple word (? like what ?)
|| 
|| need to check if the query contains the primary keys in order to
   create the groupedByMethod
   || check if column has a different name from the field